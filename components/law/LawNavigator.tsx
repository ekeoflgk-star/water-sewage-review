'use client';

/**
 * Phase 3에서 구현 예정:
 * - 법제처 Open API 연동
 * - 법률 → 시행령 → 설계기준 → 지침 계층 탐색 (파도타기)
 * - 지자체 조례 실시간 조회
 */

export function LawNavigator() {
  return (
    <div className="p-4 text-center text-xs text-slate-400">
      <p>Phase 3에서 구현 예정</p>
      <p className="mt-1">법제처 Open API 파도타기 탐색</p>
    </div>
  );
}
